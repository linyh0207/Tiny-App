<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% for (let shortUrl in urls) { %>
    <ol>
    <%= shortUrl %> : <%= urls[shortUrl] %>
    </ol>
    <% } %>
     <a href = "#">New URL</a>
</body>
</html>